<UserControl x:Class="GameUserStatistics.ScoreBoard.ScoreBoardItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" HorizontalAlignment="Stretch"
             d:DesignHeight="45" d:DesignWidth="350">
    <Border CornerRadius="5" Background="#dddddd" Margin="4" BorderBrush="#666666" HorizontalAlignment="Stretch">
        <Grid  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Label Content="01/10" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Name="lblDate" FontSize="20" Grid.Column="0" />
            <Label Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Name="lblScore" FontSize="20" Content="Score" />
            <Label Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Name="lblElapsedTime" FontSize="20" Content="Elapsed" />

        </Grid>
    </Border>
</UserControl>
